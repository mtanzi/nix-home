My home configuration using [Nix](https://nixos.org/) and [home-manager](https://github.com/rycee/home-manager).


```bash
# Clone the repo inside the `~/.config/nixpkgs` folder
❯ git clone https://github.com/mtanzi/nix-home ~/.config/nixpkgs

# Activate the configuration
❯ home-manager switch
```
